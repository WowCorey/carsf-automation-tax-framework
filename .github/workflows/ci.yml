name: CI

on:
  pull_request:
  push:
    branches:
      - main
      - "v1.5-*"

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install requirements
        run: python -m pip install -r requirements.txt

      - name: Run pytest
        run: python -m pytest

      - name: Compile Python modules
        run: python -m compileall -q model simulator scripts

      - name: Parse YAML schedules and examples
        run: |
          python - <<'PY'
          from pathlib import Path
          import yaml

          for folder in ["schedules", "examples"]:
              for path in sorted(Path(folder).rglob("*.yaml")):
                  yaml.safe_load(path.read_text(encoding="utf-8"))
                  print(f"parsed {path}")
          PY

      - name: Run worked examples
        run: python scripts/run_examples.py
